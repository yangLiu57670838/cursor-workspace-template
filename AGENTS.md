# Agent instructions

Demo banking onboarding workspace (Cursor config + product scaffold). Not production software.

## Before every implementation task

1. Read root [PRD.md](./PRD.md)
2. Use **only** the business requirements relevant to the current task
3. Do not invent product scope beyond that PRD slice

Business definitions live in `PRD.md` only — do not duplicate them here.

## Working agreements

- Keep changes small and task-scoped
- Match existing patterns under `.cursor/` and `src/`
- Prefer project-scoped rules, skills, and hooks under `.cursor/`
- Do not create skills under `~/.cursor/skills-cursor/`
